name: openvidu-test E2E

on: [push, workflow_dispatch]

jobs:
  node-docker:
    runs-on: ubuntu-latest
    steps:
       - uses: actions/checkout@v2.3.4
       - name:  Build 1
         run: |
          docker build openvidu/openvidu-test-e2e/docker
          docker run  -it openvidu/openvidu-test-e2e/docker
          ls -lart
          cd openvidu/openvidu-browser



